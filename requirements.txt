praw            # Official Reddit API wrapper
loguru          # Nice logging
backoff         # Retry with exponential backoff on transient errors
schedule        # Simple scheduler for interval jobs
orjson          # Fast JSON (used for printing events)
httpx
